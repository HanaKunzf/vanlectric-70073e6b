// Supabase client — placeholder. Will be wired via Lovable Cloud in Phase 2.
// Intentionally a stub so the rest of the codebase can import a stable shape.
export const supabase = {
  ready: false as boolean,
} as const;
